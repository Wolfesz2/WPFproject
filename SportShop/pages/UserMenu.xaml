<Page x:Class="SportShop.pages.UserMenu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SportShop.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UserMenu">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="28*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="28*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="Меню" FontSize="20px" HorizontalAlignment="Center"/>
            <Button x:Name="ProfileButton" Width="120" Content="Профиль" Margin="5" Click="ProfileButton_Click"/>
            <Button x:Name="ListButton" Width="120" Content="Товары" Margin="5" Click="ListButton_Click"/>
            <Button x:Name="BackButton" Width="120" Content="Выйти" Margin="5" Click="BackButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
